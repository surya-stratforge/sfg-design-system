{
  "name": "sfg-design-system",
  "private": true,
  "version": "0.0.1",
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:ui": "turbo run build --filter=@sfg/ui",
    "build:demo": "turbo run build --filter=demo"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
